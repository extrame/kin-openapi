This is a fine-tuned version of [https://github.com/getkin/kin-openapi](https://github.com/getkin/kin-openapi)

Why this fine-tuned version is needed:

- Try to define errors with code, so we can make following operation
- Try to change GetOperation to return errors instead of panic directly.